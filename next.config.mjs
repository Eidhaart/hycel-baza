/** @type {import('next').NextConfig} */
const nextConfig = {
  // Photos are served via <img> with public Supabase Storage URLs,
  // so no next/image remote config is required.
};

export default nextConfig;
